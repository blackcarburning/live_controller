### 2026-04-24 - Key Actions and Outcomes

- **Implemented `blackcarburning/live_controller` `/show-sync/` listener:**
    - Cloned the repository (already present in workspace).
    - Installed Python dependencies (`uvicorn`, `websockets`, `fastapi`) within a virtual environment.
    - Configured Caddy to reverse-proxy `meshlive.blackcarburning.com` to `127.0.0.1:8000`.
    - Started the `show-sync` server as a background process.
    - Required user intervention for `sudo apt install python3-pip`, `sudo apt install python3.12-venv`, and `sudo caddy reload`.
- **Provided UEA Diamondback project cost breakdown:**
    - Detailed individual hardware components, part numbers, and costs from IBM configurator outputs.
    - Confirmed VAT was not applied to the listed prices.
